# Modo Demo Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Un switch `DEMO_MODE` (preview/local) que deja probar a mano el flujo de compra simulando el pago (sin Stripe), imposible de activar en producción.

**Architecture:** `isDemoMode()` (env `DEMO_MODE` + doble guard `VERCEL_ENV`/dominio). En demo, `/api/checkout` corta antes de Stripe y devuelve la URL de `/demo-checkout`, que muestra un form; su action `simulatePurchase` construye una sesión Stripe sintética y reusa `provisionGuestPurchase` (misma función que el webhook real). `/gracias` en demo muestra un link de fijar contraseña. Un banner global avisa. Todo inerte en prod.

**Tech Stack:** Next.js 16 (route handlers, server components, server actions), Supabase admin (`@supabase/supabase-js`), Stripe types, Vitest.

## Global Constraints

- Switch = env var `DEMO_MODE`. `isDemoMode()` DEBE devolver false si `VERCEL_ENV === 'production'` O si `NEXT_PUBLIC_BASE_URL` contiene `luisysarabachatango.com`, aunque `DEMO_MODE='true'`.
- Fail-safe: sin `DEMO_MODE=true`, todo es Stripe real y las rutas demo → `notFound()`.
- `provisionGuestPurchase(session, admin)` (de `@/utils/checkout/provision-guest`) NO se modifica; se reusa con una sesión sintética `{ id:'demo_<uuid>', customer_details:{email}, metadata:{courseId}, amount_total, customer:null }`.
- No tocar el webhook. No romper la rama real (logueada/guest) de `/api/checkout`.
- Email siempre en minúsculas.
- Tests unit en `__tests__/` (node); componentes en `__tests__/components/` (jsdom, primera línea `// @vitest-environment jsdom`). Antes de cada commit: `npx tsc --noEmit` (0 err) y `npx eslint <archivos>` (0 err); suite completa verde antes del commit final de cada tarea.

---

## File Structure

- `utils/demo/mode.ts` — `isDemoMode()` (crear).
- `app/api/checkout/route.ts` — rama demo (modificar).
- `app/demo-checkout/actions.ts` — `simulatePurchase` (crear).
- `app/demo-checkout/page.tsx` — página de pago simulado (crear).
- `app/demo-checkout/demo-checkout.module.css` — estilos (crear).
- `components/DemoCheckoutForm.tsx` — form cliente (crear).
- `app/gracias/page.tsx` — rama demo (modificar).
- `components/DemoBanner.tsx` — banner (crear).
- `app/layout.tsx` — render del banner (modificar).
- `.env.local.example` — documentar `DEMO_MODE` (modificar).
- Tests: `__tests__/unit/demo-mode.test.ts`, `__tests__/api/checkout.test.ts` (extender), `__tests__/actions/simulate-purchase.test.ts`, `__tests__/api/demo-checkout.test.ts`, `__tests__/api/gracias.test.ts` (extender), `__tests__/components/demo-banner.test.tsx`.

---

### Task 1: `isDemoMode()` helper

**Files:**
- Create: `utils/demo/mode.ts`
- Test: `__tests__/unit/demo-mode.test.ts`

**Interfaces:**
- Produces: `export function isDemoMode(): boolean`. Consumido por todas las tareas siguientes.

- [ ] **Step 1: Test (falla primero)**

Crear `__tests__/unit/demo-mode.test.ts`:

```ts
import { describe, it, expect, beforeEach, afterEach } from 'vitest'
import { isDemoMode } from '@/utils/demo/mode'

const ORIG = { ...process.env }
beforeEach(() => { process.env = { ...ORIG } })
afterEach(() => { process.env = { ...ORIG } })

describe('isDemoMode', () => {
  it('false cuando DEMO_MODE no es "true"', () => {
    delete process.env.DEMO_MODE
    expect(isDemoMode()).toBe(false)
  })

  it('true en local/preview con DEMO_MODE=true y dominio no-prod', () => {
    process.env.DEMO_MODE = 'true'
    delete process.env.VERCEL_ENV
    process.env.NEXT_PUBLIC_BASE_URL = 'http://localhost:3000'
    expect(isDemoMode()).toBe(true)
  })

  it('false en VERCEL_ENV=production aunque DEMO_MODE=true', () => {
    process.env.DEMO_MODE = 'true'
    process.env.VERCEL_ENV = 'production'
    process.env.NEXT_PUBLIC_BASE_URL = 'https://preview.vercel.app'
    expect(isDemoMode()).toBe(false)
  })

  it('false si el dominio es el de producción real aunque DEMO_MODE=true', () => {
    process.env.DEMO_MODE = 'true'
    delete process.env.VERCEL_ENV
    process.env.NEXT_PUBLIC_BASE_URL = 'https://luisysarabachatango.com'
    expect(isDemoMode()).toBe(false)
  })
})
```

- [ ] **Step 2: Ejecutar — debe fallar**

Run: `npx vitest run __tests__/unit/demo-mode.test.ts`
Expected: FAIL con "Cannot find module '@/utils/demo/mode'".

- [ ] **Step 3: Implementar**

Crear `utils/demo/mode.ts`:

```ts
/**
 * Modo demo: simula el pago (sin Stripe) para probar a mano. Se controla con
 * la env var DEMO_MODE. Doble guard: NUNCA activo en producción real, aunque
 * DEMO_MODE esté a 'true'. Pensado para deploys preview o local.
 */
export function isDemoMode(): boolean {
  if (process.env.DEMO_MODE !== 'true') return false;
  if (process.env.VERCEL_ENV === 'production') return false;
  if ((process.env.NEXT_PUBLIC_BASE_URL ?? '').includes('luisysarabachatango.com')) return false;
  return true;
}
```

- [ ] **Step 4: Ejecutar — debe pasar**

Run: `npx vitest run __tests__/unit/demo-mode.test.ts`
Expected: PASS (4 casos).

- [ ] **Step 5: Typecheck + commit**

Run: `npx tsc --noEmit` → 0 errores.
```bash
git add utils/demo/mode.ts __tests__/unit/demo-mode.test.ts
git commit -m "feat(demo): helper isDemoMode con doble guard anti-producción"
```

---

### Task 2: rama demo en `/api/checkout`

**Files:**
- Modify: `app/api/checkout/route.ts`
- Test: `__tests__/api/checkout.test.ts` (añadir bloque demo)

**Interfaces:**
- Consumes: `isDemoMode` (Task 1).
- Produces: en demo, `POST /api/checkout` con `courseId` devuelve `{ url: '/demo-checkout?courseId=<id>' }` sin llamar a Stripe.

- [ ] **Step 1: Test demo (falla primero)**

Al principio de `__tests__/api/checkout.test.ts`, junto a los otros `vi.mock`, añadir un mock de isDemoMode:

```ts
const mockIsDemoMode = vi.fn(() => false)
vi.mock('@/utils/demo/mode', () => ({ isDemoMode: () => mockIsDemoMode() }))
```

Y añadir al final del archivo:

```ts
describe('POST /api/checkout — modo demo', () => {
  beforeEach(() => {
    vi.clearAllMocks()
    mockIsDemoMode.mockReturnValue(true)
    mockGetUser.mockResolvedValue({ data: { user: null } })
  })

  it('en demo, con courseId, devuelve la url de /demo-checkout sin llamar a Stripe', async () => {
    const { POST } = await import('@/app/api/checkout/route')
    const res = await POST(makeRequest({ courseId: 'course-1' }))
    expect(res.status).toBe(200)
    const json = await res.json()
    expect(json.url).toBe('/demo-checkout?courseId=course-1')
    expect(mockSessionCreate).not.toHaveBeenCalled()
  })
})
```

(Nota: como los demás tests del archivo no ponen `mockIsDemoMode.mockReturnValue(...)`, el default `false` los deja intactos; `vi.clearAllMocks()` en sus `beforeEach` resetea a la implementación `() => mockIsDemoMode()` cuyo default es false. Verifica que el mock por defecto sea false para no romper los tests previos — si `clearAllMocks` borra el `mockReturnValue`, re-aplica `mockIsDemoMode.mockReturnValue(false)` en un `beforeEach` global al inicio del archivo.)

- [ ] **Step 2: Ejecutar — debe fallar**

Run: `npx vitest run __tests__/api/checkout.test.ts`
Expected: FAIL (aún no hay rama demo; devuelve una sesión Stripe, no la url demo).

- [ ] **Step 3: Añadir la rama demo**

En `app/api/checkout/route.ts`, añadir el import junto a los demás:

```ts
import { isDemoMode } from '@/utils/demo/mode';
```

Justo DESPUÉS del bloque `if (!courseId) { return NextResponse.json({ error: 'Falta courseId' }, { status: 400 }); }` y ANTES del fetch del curso (`const { data: course } = ...`), insertar:

```ts
    // Modo demo: corta antes de Stripe. La página /demo-checkout simula el pago.
    if (isDemoMode()) {
      return NextResponse.json({ url: `/demo-checkout?courseId=${courseId}` });
    }
```

- [ ] **Step 4: Ejecutar — debe pasar**

Run: `npx vitest run __tests__/api/checkout.test.ts`
Expected: PASS (todos los casos, incluidos guest/logueado previos + demo).

- [ ] **Step 5: Typecheck/lint + commit**

Run: `npx tsc --noEmit` (0 err), `npx eslint app/api/checkout/route.ts` (0 err).
```bash
git add app/api/checkout/route.ts __tests__/api/checkout.test.ts
git commit -m "feat(demo): /api/checkout corta a /demo-checkout en modo demo"
```

---

### Task 3: action `simulatePurchase`

**Files:**
- Create: `app/demo-checkout/actions.ts`
- Test: `__tests__/actions/simulate-purchase.test.ts`

**Interfaces:**
- Consumes: `isDemoMode` (Task 1), `provisionGuestPurchase` (`@/utils/checkout/provision-guest`, ya en main).
- Produces: `export async function simulatePurchase(formData: FormData): Promise<void>` (redirige).

- [ ] **Step 1: Test (falla primero)**

Crear `__tests__/actions/simulate-purchase.test.ts`:

```ts
import { describe, it, expect, vi, beforeEach } from 'vitest'

const mockIsDemoMode = vi.fn()
vi.mock('@/utils/demo/mode', () => ({ isDemoMode: () => mockIsDemoMode() }))

const mockProvision = vi.fn().mockResolvedValue({ ok: true, userId: 'u1' })
vi.mock('@/utils/checkout/provision-guest', () => ({
  provisionGuestPurchase: (...a: unknown[]) => mockProvision(...a),
}))

const mockGetUser = vi.fn().mockResolvedValue({ data: { user: null } })
vi.mock('@/utils/supabase/server', () => ({
  createClient: vi.fn().mockResolvedValue({ auth: { getUser: mockGetUser } }),
}))

const mockCourseSingle = vi.fn().mockResolvedValue({ data: { price_eur: 199 }, error: null })
vi.mock('@supabase/supabase-js', () => ({
  createClient: vi.fn().mockReturnValue({
    from: vi.fn().mockReturnValue({
      select: vi.fn().mockReturnThis(),
      eq: vi.fn().mockReturnThis(),
      single: mockCourseSingle,
    }),
  }),
}))

const mockRedirect = vi.fn((url: string) => { throw new Error('REDIRECT:' + url) })
vi.mock('next/navigation', () => ({ redirect: (u: string) => mockRedirect(u) }))

import { simulatePurchase } from '@/app/demo-checkout/actions'

function fd(obj: Record<string, string>) {
  const f = new FormData()
  Object.entries(obj).forEach(([k, v]) => f.append(k, v))
  return f
}

beforeEach(() => vi.clearAllMocks())

describe('simulatePurchase', () => {
  it('fuera de demo: redirige a / y NO provisiona', async () => {
    mockIsDemoMode.mockReturnValue(false)
    await expect(simulatePurchase(fd({ courseId: 'c1', email: 'a@b.com' }))).rejects.toThrow('REDIRECT:/')
    expect(mockProvision).not.toHaveBeenCalled()
  })

  it('en demo: provisiona con sesión sintética y redirige a /gracias?demo=1', async () => {
    mockIsDemoMode.mockReturnValue(true)
    await expect(simulatePurchase(fd({ courseId: 'c1', email: 'Buyer@Example.com' }))).rejects.toThrow('REDIRECT:/gracias?demo=1&email=buyer%40example.com')
    const [session] = mockProvision.mock.calls[0]
    expect(session.customer_details.email).toBe('buyer@example.com')
    expect(session.metadata.courseId).toBe('c1')
    expect(String(session.id)).toMatch(/^demo_/)
  })
})
```

- [ ] **Step 2: Ejecutar — debe fallar**

Run: `npx vitest run __tests__/actions/simulate-purchase.test.ts`
Expected: FAIL con "Cannot find module '@/app/demo-checkout/actions'".

- [ ] **Step 3: Implementar**

Crear `app/demo-checkout/actions.ts`:

```ts
'use server';

import { randomUUID } from 'node:crypto';
import { redirect } from 'next/navigation';
import type Stripe from 'stripe';
import { createClient as createSupabaseAdmin } from '@supabase/supabase-js';
import { createClient } from '@/utils/supabase/server';
import { isDemoMode } from '@/utils/demo/mode';
import { provisionGuestPurchase } from '@/utils/checkout/provision-guest';

export async function simulatePurchase(formData: FormData): Promise<void> {
  // Defensa en profundidad: jamás provisiona fuera de modo demo.
  if (!isDemoMode()) {
    redirect('/');
  }

  const courseId = ((formData.get('courseId') as string | null) ?? '').trim();
  const formEmail = ((formData.get('email') as string | null) ?? '').trim().toLowerCase();

  const supabase = await createClient();
  const { data: { user } } = await supabase.auth.getUser();
  const email = (user?.email ?? formEmail).toLowerCase();

  if (!courseId || !email) {
    redirect(`/demo-checkout?courseId=${encodeURIComponent(courseId)}&error=missing`);
  }

  const admin = createSupabaseAdmin(
    process.env.NEXT_PUBLIC_SUPABASE_URL!,
    process.env.SUPABASE_SERVICE_ROLE_KEY!,
  );

  const { data: course } = await admin
    .from('courses')
    .select('price_eur')
    .eq('id', courseId)
    .eq('is_published', true)
    .single();

  const amount = course?.price_eur ? Math.round(course.price_eur * 100) : 0;

  const session = {
    id: `demo_${randomUUID()}`,
    customer_details: { email },
    metadata: { courseId },
    amount_total: amount,
    customer: null,
  } as unknown as Stripe.Checkout.Session;

  await provisionGuestPurchase(session, admin);

  redirect(`/gracias?demo=1&email=${encodeURIComponent(email)}`);
}
```

- [ ] **Step 4: Ejecutar — debe pasar**

Run: `npx vitest run __tests__/actions/simulate-purchase.test.ts`
Expected: PASS (2 casos).

- [ ] **Step 5: Typecheck/lint + commit**

Run: `npx tsc --noEmit` (0 err), `npx eslint app/demo-checkout/actions.ts` (0 err).
```bash
git add app/demo-checkout/actions.ts __tests__/actions/simulate-purchase.test.ts
git commit -m "feat(demo): action simulatePurchase reusa provisionGuestPurchase"
```

---

### Task 4: página `/demo-checkout` + form

**Files:**
- Create: `app/demo-checkout/page.tsx`, `app/demo-checkout/demo-checkout.module.css`, `components/DemoCheckoutForm.tsx`
- Test: `__tests__/api/demo-checkout.test.ts`

**Interfaces:**
- Consumes: `isDemoMode` (Task 1), `simulatePurchase` (Task 3).

- [ ] **Step 1: Test (falla primero)**

Crear `__tests__/api/demo-checkout.test.ts`:

```ts
import { describe, it, expect, vi, beforeEach } from 'vitest'

const mockIsDemoMode = vi.fn()
vi.mock('@/utils/demo/mode', () => ({ isDemoMode: () => mockIsDemoMode() }))

const mockNotFound = vi.fn(() => { throw new Error('NOT_FOUND') })
vi.mock('next/navigation', () => ({ notFound: () => mockNotFound() }))

const mockGetUser = vi.fn().mockResolvedValue({ data: { user: null } })
const mockSingle = vi.fn().mockResolvedValue({ data: { id: 'c1', title: 'Curso', price_eur: 199 }, error: null })
vi.mock('@/utils/supabase/server', () => ({
  createClient: vi.fn().mockResolvedValue({
    auth: { getUser: mockGetUser },
    from: vi.fn().mockReturnValue({ select: vi.fn().mockReturnThis(), eq: vi.fn().mockReturnThis(), single: mockSingle }),
  }),
}))

import DemoCheckoutPage from '@/app/demo-checkout/page'

beforeEach(() => vi.clearAllMocks())

describe('/demo-checkout', () => {
  it('notFound cuando NO es modo demo', async () => {
    mockIsDemoMode.mockReturnValue(false)
    await expect(DemoCheckoutPage({ searchParams: Promise.resolve({ courseId: 'c1' }) })).rejects.toThrow('NOT_FOUND')
  })

  it('en demo, renderiza (muestra el título del curso)', async () => {
    mockIsDemoMode.mockReturnValue(true)
    const el = await DemoCheckoutPage({ searchParams: Promise.resolve({ courseId: 'c1' }) })
    expect(JSON.stringify(el)).toContain('Curso')
  })
})
```

- [ ] **Step 2: Ejecutar — debe fallar**

Run: `npx vitest run __tests__/api/demo-checkout.test.ts`
Expected: FAIL (módulo no existe).

- [ ] **Step 3: Implementar el form cliente**

Crear `components/DemoCheckoutForm.tsx`:

```tsx
'use client';

import { simulatePurchase } from '@/app/demo-checkout/actions';
import styles from '@/app/demo-checkout/demo-checkout.module.css';

interface Props {
  courseId: string;
  defaultEmail: string;
}

export default function DemoCheckoutForm({ courseId, defaultEmail }: Props) {
  return (
    <form action={simulatePurchase} className={styles.form}>
      <input type="hidden" name="courseId" value={courseId} />
      <label className={styles.label} htmlFor="demo-email">Email de prueba</label>
      <input
        id="demo-email"
        name="email"
        type="email"
        required
        defaultValue={defaultEmail}
        placeholder="tu@email.com"
        className={styles.input}
      />
      <button type="submit" className={styles.button}>Simular pago</button>
      <p className={styles.note}>Modo demo: no se cobra nada. Se crea el acceso al curso como si hubieras pagado.</p>
    </form>
  );
}
```

- [ ] **Step 4: Implementar la página + CSS**

Crear `app/demo-checkout/page.tsx`:

```tsx
import type { Metadata } from 'next';
import { notFound } from 'next/navigation';
import { createClient } from '@/utils/supabase/server';
import { isDemoMode } from '@/utils/demo/mode';
import DemoCheckoutForm from '@/components/DemoCheckoutForm';
import styles from './demo-checkout.module.css';

export const metadata: Metadata = {
  title: 'Pago simulado (demo)',
  robots: { index: false, follow: false },
};

export const dynamic = 'force-dynamic';

export default async function DemoCheckoutPage(props: { searchParams: Promise<{ courseId?: string }> }) {
  if (!isDemoMode()) notFound();

  const { courseId } = await props.searchParams;
  if (!courseId) notFound();

  const supabase = await createClient();
  const { data: { user } } = await supabase.auth.getUser();
  const { data: course } = await supabase
    .from('courses')
    .select('id, title, price_eur')
    .eq('id', courseId)
    .eq('is_published', true)
    .single();

  if (!course) notFound();

  return (
    <div className={styles.wrap}>
      <div className={styles.card}>
        <span className={styles.badge}>MODO DEMO</span>
        <h1 className={styles.title}>{course.title}</h1>
        <p className={styles.price}>€{course.price_eur} · pago simulado</p>
        <DemoCheckoutForm courseId={course.id} defaultEmail={user?.email ?? ''} />
      </div>
    </div>
  );
}
```

Crear `app/demo-checkout/demo-checkout.module.css`:

```css
.wrap { min-height: 80vh; display: flex; align-items: center; justify-content: center; padding: var(--spacing-2xl) var(--page-pad); background: #050505; }
.card { max-width: 480px; width: 100%; background: var(--surface); border: 1px solid var(--primary); border-radius: var(--radius-lg); padding: var(--spacing-2xl); text-align: center; }
.badge { display: inline-block; background: var(--secondary); color: #fff; font: var(--small); padding: 0.2rem 0.7rem; border-radius: var(--radius-pill); margin-bottom: var(--spacing-lg); }
.title { font: var(--h3); color: var(--text-main); margin-bottom: var(--spacing-sm); }
.price { color: var(--primary); font: var(--h4); margin-bottom: var(--spacing-lg); }
.form { display: flex; flex-direction: column; gap: var(--spacing-sm); text-align: left; }
.label { font: var(--small); color: var(--text-muted); }
.input { padding: 0.7rem 1rem; background: var(--background); border: 1px solid var(--border); border-radius: var(--radius-md); color: var(--text-main); font: var(--body); }
.button { margin-top: var(--spacing-sm); padding: 0.9rem 2rem; background: var(--primary); color: #050505; border: none; border-radius: var(--radius-pill); font: var(--h4); cursor: pointer; }
.button:hover { background: var(--primary-hover); }
.note { font: var(--small); color: var(--text-muted); margin-top: var(--spacing-sm); }
```

- [ ] **Step 5: Ejecutar test — debe pasar**

Run: `npx vitest run __tests__/api/demo-checkout.test.ts`
Expected: PASS (2 casos).

- [ ] **Step 6: Typecheck/lint + commit**

Run: `npx tsc --noEmit` (0 err), `npx eslint app/demo-checkout/ components/DemoCheckoutForm.tsx` (0 err).
```bash
git add app/demo-checkout/ components/DemoCheckoutForm.tsx __tests__/api/demo-checkout.test.ts
git commit -m "feat(demo): página /demo-checkout + form de pago simulado"
```

---

### Task 5: rama demo en `/gracias`

**Files:**
- Modify: `app/gracias/page.tsx`
- Test: `__tests__/api/gracias.test.ts` (añadir casos demo)

**Interfaces:**
- Consumes: `isDemoMode` (Task 1).

- [ ] **Step 1: Test demo (falla primero)**

En `__tests__/api/gracias.test.ts`, añadir junto a los mocks del head:

```ts
const mockIsDemoMode = vi.fn(() => false)
vi.mock('@/utils/demo/mode', () => ({ isDemoMode: () => mockIsDemoMode() }))

const mockGenerateLink = vi.fn().mockResolvedValue({ data: { properties: { action_link: 'https://sb/reset-link' } }, error: null })
vi.mock('@supabase/supabase-js', () => ({
  createClient: vi.fn().mockReturnValue({ auth: { admin: { generateLink: mockGenerateLink } } }),
}))
```

Y añadir el caso:

```ts
it('en demo con ?demo=1&email muestra el link de fijar contraseña, sin llamar a Stripe', async () => {
  mockIsDemoMode.mockReturnValue(true)
  const el = await GraciasPage({ searchParams: Promise.resolve({ demo: '1', email: 'buyer@example.com' }) })
  const html = JSON.stringify(el)
  expect(html).toContain('buyer@example.com')
  expect(html).toContain('https://sb/reset-link')
  expect(mockRetrieve).not.toHaveBeenCalled()
})
```

- [ ] **Step 2: Ejecutar — debe fallar**

Run: `npx vitest run __tests__/api/gracias.test.ts`
Expected: FAIL (no hay rama demo aún).

- [ ] **Step 3: Añadir la rama demo a `/gracias`**

Reemplazar `app/gracias/page.tsx` por (añade imports + rama demo antes de la lógica Stripe; el resto queda igual):

```tsx
import type { Metadata } from 'next';
import { stripe } from '@/utils/stripe/server';
import { createClient as createSupabaseAdmin } from '@supabase/supabase-js';
import { isDemoMode } from '@/utils/demo/mode';
import styles from './gracias.module.css';

export const metadata: Metadata = {
  title: 'Gracias por tu compra',
  robots: { index: false, follow: false },
};

export const dynamic = 'force-dynamic';

export default async function GraciasPage(props: { searchParams: Promise<{ session_id?: string; demo?: string; email?: string }> }) {
  const { session_id, demo, email: demoEmail } = await props.searchParams;

  // ── Rama demo: pago simulado, sin Stripe. Muestra el link para fijar
  // contraseña (así se prueba el flujo sin depender del email SMTP). Solo en
  // modo demo (en prod isDemoMode() es false y esta rama nunca se ejecuta).
  if (isDemoMode() && demo === '1' && demoEmail) {
    let setPasswordLink: string | null = null;
    try {
      const admin = createSupabaseAdmin(
        process.env.NEXT_PUBLIC_SUPABASE_URL!,
        process.env.SUPABASE_SERVICE_ROLE_KEY!,
      );
      const { data } = await admin.auth.admin.generateLink({
        type: 'recovery',
        email: demoEmail,
        options: { redirectTo: `${process.env.NEXT_PUBLIC_BASE_URL ?? ''}/auth/callback?next=/reset-password` },
      });
      setPasswordLink = data?.properties?.action_link ?? null;
    } catch {
      // generateLink falló → la compra ya está registrada; mostramos aviso sin link.
    }
    return (
      <div className={styles.wrap}>
        <div className={styles.card}>
          <h1 className={styles.title}>MODO DEMO · pago simulado ✅</h1>
          <p className={styles.body}>Acceso creado para <strong>{demoEmail}</strong>.</p>
          {setPasswordLink
            ? <p className={styles.hint}>Fija tu contraseña aquí: <a className={styles.link} href={setPasswordLink}>fijar contraseña</a></p>
            : <p className={styles.hint}>Cuenta creada; revisa tu email para fijar la contraseña.</p>}
          <a href="/curso-bachatango" className={styles.link}>Volver</a>
        </div>
      </div>
    );
  }

  let email: string | null = null;
  let paid = false;
  if (session_id) {
    try {
      const session = await stripe.checkout.sessions.retrieve(session_id);
      paid = session.payment_status === 'paid';
      email = session.customer_details?.email ?? null;
    } catch {
      // sesión inválida/expirada → mensaje neutro
    }
  }

  return (
    <div className={styles.wrap}>
      <div className={styles.card}>
        {paid ? (
          <>
            <h1 className={styles.title}>¡Pago recibido! 🎉</h1>
            <p className={styles.body}>
              {email
                ? <>Te hemos enviado un email a <strong>{email}</strong> para crear tu acceso al curso.</>
                : <>Te hemos enviado un email para crear tu acceso al curso.</>}
            </p>
            <p className={styles.hint}>Revisa tu bandeja de entrada (y la carpeta de spam). Pulsa el enlace para fijar tu contraseña y entrar.</p>
          </>
        ) : (
          <>
            <h1 className={styles.title}>Gracias</h1>
            <p className={styles.body}>Si has completado un pago, en breve recibirás un email con tu acceso.</p>
            <p className={styles.hint}>¿Algún problema? Escríbenos desde la página de contacto.</p>
          </>
        )}
        <a href="/curso-bachatango" className={styles.link}>Volver</a>
      </div>
    </div>
  );
}
```

- [ ] **Step 4: Ejecutar test — debe pasar**

Run: `npx vitest run __tests__/api/gracias.test.ts`
Expected: PASS (casos previos + demo).

- [ ] **Step 5: Typecheck/lint + commit**

Run: `npx tsc --noEmit` (0 err), `npx eslint app/gracias/page.tsx` (0 err).
```bash
git add app/gracias/page.tsx __tests__/api/gracias.test.ts
git commit -m "feat(demo): /gracias muestra link set-password en modo demo"
```

---

### Task 6: banner global + layout + env doc

**Files:**
- Create: `components/DemoBanner.tsx`
- Modify: `app/layout.tsx`, `.env.local.example`
- Test: `__tests__/components/demo-banner.test.tsx`

**Interfaces:**
- Consumes: `isDemoMode` (Task 1) en el layout.

- [ ] **Step 1: Test del banner (falla primero)**

Crear `__tests__/components/demo-banner.test.tsx`:

```tsx
// @vitest-environment jsdom
import { describe, it, expect } from 'vitest'
import { render, screen } from '@testing-library/react'
import DemoBanner from '@/components/DemoBanner'

describe('DemoBanner', () => {
  it('muestra el aviso de modo demo', () => {
    render(<DemoBanner />)
    expect(screen.getByText(/MODO DEMO/i)).toBeInTheDocument()
  })
})
```

- [ ] **Step 2: Ejecutar — debe fallar**

Run: `npx vitest run __tests__/components/demo-banner.test.tsx`
Expected: FAIL (módulo no existe).

- [ ] **Step 3: Implementar el banner**

Crear `components/DemoBanner.tsx`:

```tsx
export default function DemoBanner() {
  return (
    <div
      role="status"
      style={{
        background: '#8a1c1c',
        color: '#fff',
        textAlign: 'center',
        padding: '0.5rem 1rem',
        fontSize: '0.875rem',
        fontWeight: 600,
        position: 'relative',
        zIndex: 100,
      }}
    >
      ⚠️ MODO DEMO — los pagos son simulados, no se cobra nada
    </div>
  );
}
```

- [ ] **Step 4: Renderizar en el layout**

En `app/layout.tsx`, añadir el import junto a los demás componentes:

```tsx
import DemoBanner from '@/components/DemoBanner';
import { isDemoMode } from '@/utils/demo/mode';
```

Dentro del `<body>`, justo después de `<LanguageProvider>` y ANTES de `<Header ... />` (localiza `<Header user={user} profile={profile} />` y coloca la línea antes), insertar:

```tsx
          {isDemoMode() && <DemoBanner />}
```

- [ ] **Step 5: Documentar la env var**

En `.env.local.example`, añadir al final:

```
# Modo demo (solo preview/local): simula el pago sin Stripe para probar a mano.
# NUNCA poner en producción (el código lo bloquea igualmente en el dominio real).
DEMO_MODE=false
```

- [ ] **Step 6: Ejecutar test + suite + build**

Run: `npx vitest run __tests__/components/demo-banner.test.tsx` → PASS.
Run: `npx tsc --noEmit` (0 err), `npm run lint` (0 err en archivos de la rama), `npx vitest run` (toda la suite verde), `npm run build` (compila).

- [ ] **Step 7: Verificación manual (demo ON)**

Run: `DEMO_MODE=true npm run dev`, abrir `http://localhost:3000/curso-bachatango`.
Comprobar:
- Banner "MODO DEMO" visible arriba.
- CTA "Comprar" → redirige a `/demo-checkout` (no a Stripe).
- Form con email → "Simular pago" → `/gracias?demo=1` con link de fijar contraseña.
- Pulsar el link → `/reset-password` → fijar contraseña → acceso.
- Parar; arrancar `npm run dev` SIN `DEMO_MODE` → CTA vuelve a Stripe real, `/demo-checkout` → 404, banner oculto.

- [ ] **Step 8: Commit**

```bash
git add components/DemoBanner.tsx app/layout.tsx .env.local.example __tests__/components/demo-banner.test.tsx
git commit -m "feat(demo): banner global de modo demo + doc env DEMO_MODE"
```

---

## Self-Review (autor del plan)

**Spec coverage:**
- `isDemoMode()` doble guard → Task 1.
- `/api/checkout` rama demo → Task 2.
- `simulatePurchase` + sesión sintética + reuso de `provisionGuestPurchase` → Task 3.
- `/demo-checkout` page + form (notFound si no demo) → Task 4.
- `/gracias` rama demo con link set-password → Task 5.
- Banner global + env doc → Task 6.
- Fail-safe a producción → Tasks 1 (guard) + 2/4/5 (todas gated por isDemoMode) + verificación manual Task 6 Step 7.

**Placeholder scan:** sin TBD/TODO; todo el código está completo.

**Type consistency:** `isDemoMode(): boolean` consistente en todas las tareas. `simulatePurchase(formData): Promise<void>` y `provisionGuestPurchase(session, admin)` (sesión sintética con `customer_details.email`, `metadata.courseId`, `id 'demo_'`, `amount_total`, `customer:null`) — la forma de la sesión coincide con lo que `provisionGuestPurchase` lee (verificado contra su implementación en main). `DemoCheckoutForm {courseId, defaultEmail}` consistente con la página.

**Nota:** el `provisionGuestPurchase` real llama a `inviteUserByEmail` para emails nuevos → en demo envía un invite real (además del link mostrado en /gracias). Es el comportamiento deseado (prueba el flujo). Con SMTP no configurado, el invite no llega pero el link de /gracias permite completar la prueba.
